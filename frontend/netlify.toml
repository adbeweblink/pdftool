[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NEXT_PUBLIC_API_URL = "https://pdftool-backend.onrender.com"

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
